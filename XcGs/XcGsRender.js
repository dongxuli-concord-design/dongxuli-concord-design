class XcGsRender {
  canvas;
  width;
  height;
  constructor({canvas, width, height}) {
    this.canvas = canvas;
    this.width = width;
    this.height = height;
  }

  clear() {

  }

  render({scene, camera}) {

  }
}
